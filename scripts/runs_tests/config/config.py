# biofilter config init

# biofilter config init \
#   --db-uri postgresql+psycopg2://biousers:biousers@localhost/biofilter_dev

# biofilter config init --path /opt/biofilter/dev

# biofilter config init --force

# biofilter config init \
#   --db-uri sqlite:///biofilter.db \
#   --data-root /data/biofilter

# biofilter config show

# biofilter --db-uri sqlite:///tmp/test.db config show

# biofilter config get database.db_uri

# biofilter config set database.db_uri \
#   postgresql+psycopg2://bioadmin:bioadmin@localhost/biofilter_prod

# This module need to be run by CLI
